# Legacy / unused UI

These modules are not imported by the active app (`src/app/page.tsx` and its dependency tree). They are
kept for reference or future reuse. Do not import from here in production routes without moving the code
back under `src/components/`.
